name: postoplan_test
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^3.1.0
  flutter_login: ^1.1.0
  flutter_svg: ^0.22.0
  get: ^4.3.8
  get_storage: ^2.0.3
  google_fonts: ^2.1.0
  http: ^0.13.3
  openapi_dart_common: ^4.1.0
  openapi_generator_annotations: ^3.2.0
  postoplan_api:
    path: api/postoplan_api
  responsive_sizer: ^3.0.4+4
  select_dialog: ^1.2.3

dev_dependencies:
  build_runner: ^2.0.6
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter
  openapi_generator: 3.2.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/svg/
    - .env
